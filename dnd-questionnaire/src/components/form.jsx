import AnswerQuestion from "./answerquestions"

const Form = () => {

    return (
        <h1 className='new-Question' onClick={console.log('hiddd')}>Click for Survey</h1>

    )
}

export default Form